{
  "name": "@mcp-page-bridge/example-svelte",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Svelte 5 example that exposes its runes state to the agent via WebMCP (document.modelContext)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.1",
    "vite": "^8.0.16",
    "webmcp-types": "^0.1.5"
  }
}
