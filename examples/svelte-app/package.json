{
  "name": "@mcp-page-bridge/example-svelte",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Svelte 5 example that exposes its runes state to the agent via window.mcp",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.1",
    "vite": "^8.0.16"
  }
}
