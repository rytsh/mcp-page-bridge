# Build artifact committed so `go install` works from a clean checkout;
# regenerated by `pnpm --filter mcp-page-bridge-dashboard build`.
internal/server/assets/dashboard.html linguist-generated=true
